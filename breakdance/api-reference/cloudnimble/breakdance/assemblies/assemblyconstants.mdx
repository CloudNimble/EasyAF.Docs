---
title: "AssemblyConstants"
description: ""
icon: "cube"
---

# AssemblyConstants

<Badge variant="outline">Class</Badge>

## Syntax

```csharp
public static class AssemblyConstants
```

## Inheritance

- [object](https://learn.microsoft.com/dotnet/api/system.object)

## Fields

### Abstract

```csharp
public const string Abstract = "abstract"
```

Used to represent the "abstract" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Add

```csharp
public const string Add = "add"
```

Used to represent the "add" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### BraceClose

```csharp
public const string BraceClose = "}"
```

Used to represent an close brace ("}").

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### BraceOpen

```csharp
public const string BraceOpen = "{"
```

Used to represent an open brace ("{").

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### BracketClose

```csharp
public const string BracketClose = "]"
```

Used to represent an close bracket ("]").

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### BracketOpen

```csharp
public const string BracketOpen = "["
```

Used to represent an open bracket ("[").

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Class

```csharp
public const string Class = "class"
```

Used to represent the "class" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Colon

```csharp
public const string Colon = " : "
```

Used to represent the "abstract" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Const

```csharp
public const string Const = "const"
```

Used to represent the "const" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Enum

```csharp
public const string Enum = "enum"
```

Used to represent the "enum" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Get

```csharp
public const string Get = "get"
```

Used to represent the "get" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Interface

```csharp
public const string Interface = "interface"
```

Used to represent the "interface" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Internal

```csharp
public const string Internal = "internal"
```

Used to represent the "internal" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Out

```csharp
public const string Out = "out"
```

Used to represent the "out" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Params

```csharp
public const string Params = "params"
```

Used to represent the "params" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### ParenthesisClose

```csharp
public const string ParenthesisClose = ")"
```

Used to represent an close parenthesis (")").

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### ParenthesisOpen

```csharp
public const string ParenthesisOpen = "("
```

Used to represent an an open parenthesis ("(").

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Private

```csharp
public const string Private = "private"
```

Used to represent the "private" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Protected

```csharp
public const string Protected = "protected"
```

Used to represent the "protected" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Public

```csharp
public const string Public = "public"
```

Used to represent the "public" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### ReadOnly

```csharp
public const string ReadOnly = "readonly"
```

Used to represent the "readonly" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Remove

```csharp
public const string Remove = "remove"
```

Used to represent the "remove" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Sealed

```csharp
public const string Sealed = "sealed"
```

Used to represent the "sealed" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Set

```csharp
public const string Set = "set"
```

Used to represent the "set" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Space

```csharp
public const string Space = " "
```

Used to represent an space (" ").

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Static

```csharp
public const string Static = "static"
```

Used to represent the "static" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Struct

```csharp
public const string Struct = "struct"
```

Used to represent the "struct" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Unknown

```csharp
public const string Unknown = "UNKNOWN"
```

Used to represent the "UNKNOWN" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)

### Virtual

```csharp
public const string Virtual = "virtual"
```

Used to represent the "virtual" keyword.

#### Returns

[string](https://learn.microsoft.com/dotnet/api/system.string)


